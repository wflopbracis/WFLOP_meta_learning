#ifndef STATISTICAL_FUNCTIONS_H
#define STATISTICAL_FUNCTIONS_H

#include <vector>

double mean(const std::vector<double>& values);

double sd(const std::vector<double>& values);

double r1(const std::vector<double>& values);

double kr(const std::vector<double>& values);

double sk(const std::vector<double>& values);

#endif 
